{
  "scripts": {
    "mocha": "mocha -r ts-node/register tests/*.spec.ts --timeout 10000 --exit",
    "test": "firebase emulators:exec --only firestore \"npm run-script mocha\""
  },
  "devDependencies": {
    "@firebase/firestore": "^3.4.11",
    "@firebase/rules-unit-testing": "^2.0.3",
    "@types/mocha": "^9.1.1",
    "dotenv": "^16.0.1",
    "mocha": "^10.0.0",
    "ts-node": "^10.8.1"
  }
}